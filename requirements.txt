# Core
discord.py>=2.3,<3
python-jobspy>=1.1
sqlalchemy>=2.0,<3
aiosqlite>=0.19
apscheduler>=3.10,<4
pandas>=2.0
pysocks>=1.7
python-dotenv>=1.0
numpy>=1.24

# Semantic matching (optional — falls back to regex if missing)
sentence-transformers>=3.0

# Web dashboard + public API
fastapi>=0.100
uvicorn>=0.20
jinja2>=3.1
python-multipart>=0.0.6
httpx>=0.24
itsdangerous>=2.1

# Postgres driver (prod database; SQLite works for dev)
asyncpg>=0.29

# Email delivery (optional)
# aiosmtplib>=2.0

# Webhook delivery (optional)
# aiohttp>=3.9

# CV parsing for cover letter generation
pymupdf>=1.24
